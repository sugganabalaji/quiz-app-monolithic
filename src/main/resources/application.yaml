spring:
  application:
    name: quiz-application

  datasource:
    url: jdbc:postgresql://localhost:5432/Dev
    username: postgres
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true